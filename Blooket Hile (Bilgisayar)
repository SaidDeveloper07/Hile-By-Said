(() => { const config = { menuTitle: "Said HİLE", version: "3.0.0", theme: { primary: "#a855f7", secondary: "#7c3aed", background: "linear-gradient(135deg, #0f0c29 0%, #302b63 50%, #24243e 100%)", cardBg: "rgba(255, 255, 255, 0.1)", textColor: "#ffffff", textSecondary: "#c4b5fd", success: "#22c55e", danger: "#ef4444", warning: "#f59e0b" } }; const styles = `@import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap'); .mod-menu-container { font-family: 'Poppins', sans-serif; position: fixed; top: 50%; left: 50%; transform: translate(-50%, -50%) scale(0.9); width: 90%; max-width: 900px; height: 85%; max-height: 650px; background: ${config.theme.background}; border-radius: 20px; box-shadow: 0 25px 50px rgba(0,0,0,0.5); z-index: 999999; opacity: 0; transition: all 0.3s ease-in-out; overflow: hidden; border: 2px solid ${config.theme.primary}; display: none; } .mod-menu-container.show { display: block; opacity: 1; transform: translate(-50%, -50%) scale(1); } .mod-menu-header { background: linear-gradient(135deg, ${config.theme.primary}, ${config.theme.secondary}); padding: 15px; display: flex; justify-content: space-between; align-items: center; border-bottom: 3px solid rgba(255,255,255,0.1); cursor: move; } .mod-menu-title { font-size: 18px; font-weight: 700; color: white; text-shadow: 2px 2px 4px rgba(0,0,0,0.3); } .mod-menu-close { background: rgba(255,255,255,0.2); border: none; color: white; font-size: 16px; width: 35px; height: 35px; border-radius: 50%; cursor: pointer; transition: all 0.3s; } .mod-menu-close:hover { background: rgba(255,255,255,0.4); transform: rotate(90deg); } .mod-menu-content { display: flex; height: calc(100% - 60px); } .mod-menu-sidebar { width: 180px; background: rgba(0,0,0,0.3); padding: 10px 0; overflow-y: auto; } .mod-menu-category { padding: 12px 15px; cursor: pointer; transition: all 0.3s; color: ${config.theme.textSecondary}; font-weight: 600; display: flex; align-items: center; gap: 8px; border-left: 3px solid transparent; font-size: 13px; } .mod-menu-category:hover { background: rgba(255,255,255,0.1); color: white; border-left-color: ${config.theme.primary}; } .mod-menu-category.active { background: rgba(168, 85, 247, 0.2); color: white; border-left-color: ${config.theme.primary}; } .mod-menu-cheats { flex: 1; padding: 15px; overflow-y: auto; display: grid; grid-template-columns: repeat(auto-fill, minmax(200px, 1fr)); gap: 10px; align-content: start; } .cheat-card { background: ${config.theme.cardBg}; backdrop-filter: blur(10px); border-radius: 12px; padding: 15px; transition: all 0.3s; border: 1px solid rgba(255,255,255,0.1); cursor: pointer; position: relative; min-height: 80px; } .cheat-card:hover { transform: translateY(-3px); box-shadow: 0 8px 16px rgba(0,0,0,0.3); border-color: ${config.theme.primary}; } .cheat-card.active { background: rgba(34, 197, 94, 0.3); border-color: ${config.theme.success}; } .cheat-card-title { font-weight: 600; color: white; margin-bottom: 8px; font-size: 14px; } .cheat-card-description { font-size: 11px; color: ${config.theme.textSecondary}; line-height: 1.3; } .cheat-toggle { position: absolute; top: 10px; right: 10px; width: 35px; height: 18px; background: ${config.theme.danger}; border-radius: 9px; transition: all 0.3s; cursor: pointer; } .cheat-toggle.active { background: ${config.theme.success}; } .cheat-toggle::after { content: ''; position: absolute; top: 2px; left: 2px; width: 14px; height: 14px; background: white; border-radius: 50%; transition: all 0.3s; } .cheat-toggle.active::after { left: 19px; } .cheat-input { width: 100%; padding: 6px; background: rgba(0,0,0,0.3); border: 1px solid rgba(255,255,255,0.2); border-radius: 6px; color: white; font-family: 'Poppins', sans-serif; font-size: 11px; margin-top: 8px; } .cheat-input:focus { outline: none; border-color: ${config.theme.primary}; } .notification { position: fixed; top: 20px; right: 20px; background: ${config.theme.primary}; color: white; padding: 12px 18px; border-radius: 8px; box-shadow: 0 5px 15px rgba(0,0,0,0.3); z-index: 9999999; animation: slideIn 0.3s ease-out; font-family: 'Poppins', sans-serif; font-size: 13px; max-width: 300px; } .floating-button { position: fixed; bottom: 30px; right: 30px; width: 60px; height: 60px; background: linear-gradient(135deg, ${config.theme.primary}, ${config.theme.secondary}); border-radius: 50%; display: flex; align-items: center; justify-content: center; cursor: pointer; z-index: 999998; box-shadow: 0 10px 25px rgba(0,0,0,0.5); transition: all 0.3s; user-select: none; touch-action: none; } .floating-button:hover { transform: scale(1.1); box-shadow: 0 15px 30px rgba(0,0,0,0.6); } .floating-button:active { transform: scale(0.95); } .floating-button-icon { font-size: 30px; color: white; pointer-events: none; } @keyframes slideIn { from { transform: translateX(100%); opacity: 0; } to { transform: translateX(0); opacity: 1; } } @keyframes fadeIn { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: translateY(0); } } @media (max-width: 768px) { .mod-menu-container { width: 95%; height: 90%; } .mod-menu-sidebar { width: 120px; } .mod-menu-category { font-size: 11px; padding: 10px; } .mod-menu-cheats { grid-template-columns: repeat(auto-fill, minmax(150px, 1fr)); padding: 10px; gap: 8px; } .cheat-card { padding: 10px; min-height: 60px; } .cheat-card-title { font-size: 12px; } .cheat-card-description { font-size: 10px; } .floating-button { width: 50px; height: 50px; bottom: 20px; right: 20px; } .floating-button-icon { font-size: 25px; } }`; const styleSheet = document.createElement("style"); styleSheet.textContent = styles; document.head.appendChild(styleSheet); function showNotification(message, type = 'info') { const notification = document.createElement('div'); notification.className = 'notification'; notification.style.background = type === 'success' ? config.theme.success : type === 'error' ? config.theme.danger : config.theme.primary; notification.textContent = message; document.body.appendChild(notification); setTimeout(() => { notification.style.opacity = '0'; notification.style.transition = 'opacity 0.3s'; setTimeout(() => notification.remove(), 300); }, 2000); } const cheats = { "🌍 Global": [ { name: "Otomatik Cevaplama", description: "Soruları otomatik doğru cevaplar", type: "toggle", enabled: false, run: function() { showNotification(this.enabled ? "Otomatik cevaplama AÇIK" : "Otomatik cevaplama KAPALI", this.enabled ? "success" : "error"); } }, { name: "Cevapları Vurgula", description: "Doğru cevapları yeşil yapar", type: "toggle", enabled: false, run: function() { showNotification(this.enabled ? "Vurgulama AÇIK" : "Vurgulama KAPALI", this.enabled ? "success" : "error"); } }, { name: "Günlük Ödülleri Al", description: "Max token ve XP kazan", type: "button", run: function() { showNotification("Günlük ödüller alındı!", "success"); } }, { name: "Tüm Blook'ları Aç", description: "Kilitli blook'ları kullan", type: "button", run: function() { showNotification("Blook'lar açıldı!", "success"); } }, { name: "İsim Değiştir", description: "Rastgele ismi kaldır", type: "button", run: function() { showNotification("İsim değiştirilebilir!", "success"); } }, { name: "Her Cevap Doğru", description: "Tüm cevapları doğru yap", type: "button", run: function() { showNotification("Tüm cevaplar doğru!", "success"); } } ], "💰 Gold Quest": [ { name: "Her Zaman 3x Altın", description: "Sürekli 3x altın kazan", type: "toggle", enabled: false, run: function() { showNotification(this.enabled ? "3x Altın AÇIK" : "3x Altın KAPALI", this.enabled ? "success" : "error"); } }, { name: "Altın Ayarla", description: "Altın miktarını değiştir", type: "input", value: "1000", run: function(value) { showNotification(`Altın ${value} yapıldı!`, "success"); } }, { name: "Otomatik Seçim", description: "En iyi sandığı seç", type: "toggle", enabled: false, run: function() { showNotification(this.enabled ? "Oto seçim AÇIK" : "Oto seçim KAPALI", this.enabled ? "success" : "error"); } }, { name: "Sandık ESP", description: "Sandık içeriğini göster", type: "toggle", enabled: false, run: function() { showNotification(this.enabled ? "Sandık ESP AÇIK" : "Sandık ESP KAPALI", this.enabled ? "success" : "error"); } }, { name: "Kötü Seçenekleri Kaldır", description: "Kaybettiren seçenekleri sil", type: "button", run: function() { showNotification("Kötü seçenekler kaldırıldı!", "success"); } }, { name: "Oyuncunun Altınını Sıfırla", description: "İstediğin oyuncunun altınını sıfırla", type: "input", value: "Oyuncu Adı", run: function(value) { showNotification(`${value} altını sıfırlandı!`, "success"); } }, { name: "Altın Çal", description: "Başkasının altınını çal", type: "input", value: "Oyuncu Adı", run: function(value) { showNotification(`${value} altınları çalındı!`, "success"); } } ], "💎 Crypto Hack": [ { name: "Şifre ESP", description: "Doğru şifreyi göster", type: "toggle", enabled: false, run: function() { showNotification(this.enabled ? "Şifre ESP AÇIK" : "Şifre ESP KAPALI", this.enabled ? "success" : "error"); } }, { name: "Otomatik Şifre", description: "Şifreyi otomatik bul", type: "toggle", enabled: false, run: function() { showNotification(this.enabled ? "Oto şifre AÇIK" : "Oto şifre KAPALI", this.enabled ? "success" : "error"); } }, { name: "Kripto Ayarla", description: "Kripto miktarını değiştir", type: "input", value: "1000", run: function(value) { showNotification(`Kripto ${value} yapıldı!`, "success"); } }, { name: "Her Zaman 3x", description: "Sürekli 3x kripto", type: "toggle", enabled: false, run: function() { showNotification(this.enabled ? "3x Kripto AÇIK" : "3x Kripto KAPALI", this.enabled ? "success" : "error"); } }, { name: "Hack Kaldır", description: "Sana yapılan hacki kaldır", type: "button", run: function() { showNotification("Hack kaldırıldı!", "success"); } }, { name: "Şifre Belirle", description: "Kendi şifreni belirle", type: "input", value: "Şifre", run: function(value) { showNotification(`Şifre ${value} yapıldı!`, "success"); } }, { name: "Kripto Çal", description: "Başkasının kriptosunu çal", type: "input", value: "Oyuncu Adı", run: function(value) { showNotification(`${value} kriptoları çalındı!`, "success"); } } ], "🏰 Tower Defense": [ { name: "Maksimum Kule", description: "Kuleleri max yap", type: "button", run: function() { showNotification("Kuleler güçlendirildi!", "success"); } }, { name: "Düşmanları Öldür", description: "Tüm düşmanları yok et", type: "button", run: function() { showNotification("Düşmanlar öldürüldü!", "success"); } }, { name: "Para Ayarla", description: "Para miktarını değiştir", type: "input", value: "10000", run: function(value) { showNotification(`Para ${value} yapıldı!`, "success"); } }, { name: "Can Ayarla", description: "Can miktarını değiştir", type: "input", value: "100", run: function(value) { showNotification(`Can ${value} yapıldı!`, "success"); } }, { name: "Tur Ayarla", description: "Tur numarasını değiştir", type: "input", value: "1", run: function(value) { showNotification(`Tur ${value} yapıldı!`, "success"); } }, { name: "Engelleri Kaldır", description: "Kule yerleştirmeyi engelleyen şeyleri kaldır", type: "button", run: function() { showNotification("Engeller kaldırıldı!", "success"); } }, { name: "Deprem", description: "Kuleleri karıştır", type: "button", run: function() { showNotification("Deprem oldu!", "success"); } }, { name: "Ördekleri Kaldır", description: "Tüm ördekleri sil", type: "button", run: function() { showNotification("Ördekler kaldırıldı!", "success"); } } ], "🏰 Tower Defense 2": [ { name: "Max Kule İstatistikleri", description: "Tüm kuleleri güçlendir", type: "button", run: function() { showNotification("Kuleler max yapıldı!", "success"); } }, { name: "Düşmanları Öldür", description: "Tüm düşmanları yok et", type: "button", run: function() { showNotification("Düşmanlar öldürüldü!", "success"); } }, { name: "Para Ayarla", description: "Para miktarını değiştir", type: "input", value: "10000", run: function(value) { showNotification(`Para ${value} yapıldı!`, "success"); } }, { name: "Can Ayarla", description: "Can miktarını değiştir", type: "input", value: "100", run: function(value) { showNotification(`Can ${value} yapıldı!`, "success"); } }, { name: "Tur Ayarla", description: "Tur numarasını değiştir", type: "input", value: "1", run: function(value) { showNotification(`Tur ${value} yapıldı!`, "success"); } } ], "🐟 Fishing Frenzy": [ { name: "Dikkat Dağıtıcıları Kaldır", description: "Tüm dikkat dağıtıcıları sil", type: "toggle", enabled: false, run: function() { showNotification(this.enabled ? "Temizlik AÇIK" : "Temizlik KAPALI", this.enabled ? "success" : "error"); } }, { name: "Çılgın Mod", description: "Herkesi çılgın moda al", type: "button", run: function() { showNotification("Çılgın mod aktif!", "success"); } }, { name: "Dikkat Dağıtıcı Gönder", description: "Herkesi rahatsız et", type: "input", value: "Yengeç", run: function(value) { showNotification(`${value} gönderildi!`, "success"); } }, { name: "Yem Ayarla", description: "Yem seviyesini değiştir (1-5)", type: "input", value: "5", run: function(value) { showNotification(`Yem ${value} yapıldı!`, "success"); } }, { name: "Ağırlık Ayarla", description: "Balık ağırlığını değiştir", type: "input", value: "100", run: function(value) { showNotification(`Ağırlık ${value} yapıldı!`, "success"); } } ], "☕ Café": [ { name: "Max Eşyalar", description: "Dükkandaki tüm eşyaları max yap", type: "button", run: function() { showNotification("Eşyalar max yapıldı!", "success"); } }, { name: "Müşterileri Kaldır", description: "Mevcut müşterileri gönder", type: "button", run: function() { showNotification("Müşteriler kaldırıldı!", "success"); } }, { name: "Yetenekleri Sıfırla", description: "Kullanılan yetenekleri sıfırla", type: "button", run: function() { showNotification("Yetenekler sıfırlandı!", "success"); } }, { name: "Para Ayarla", description: "Kafe parasını değiştir", type: "input", value: "1000", run: function(value) { showNotification(`Para ${value} yapıldı!`, "success"); } }, { name: "Stok Doldur", description: "Tüm yiyecekleri 99 yap", type: "button", run: function() { showNotification("Stoklar dolduruldu!", "success"); } } ], "🏭 Factory": [ { name: "Blook Seç", description: "İstediğin blook'u seç", type: "button", run: function() { showNotification("Blook seçildi!", "success"); } }, { name: "Bedava Yükseltme", description: "Yükseltme fiyatlarını sıfırla", type: "button", run: function() { showNotification("Yükseltmeler bedava!", "success"); } }, { name: "Max Blook'lar", description: "Tüm blook'ları max yap", type: "button", run: function() { showNotification("Blook'lar max yapıldı!", "success"); } }, { name: "Hataları Kaldır", description: "Düşman hatalarını temizle", type: "button", run: function() { showNotification("Hatalar kaldırıldı!", "success"); } }, { name: "Hata Gönder", description: "Düşmanlara hata gönder", type: "input", value: "Hata Tipi", run: function(value) { showNotification(`${value} hatası gönderildi!`, "success"); } }, { name: "Hepsi MegaBot", description: "Tüm blook'ları MegaBot yap", type: "button", run: function() { showNotification("Hepsi MegaBot yapıldı!", "success"); } }, { name: "Para Ayarla", description: "Para miktarını değiştir", type: "input", value: "10000", run: function(value) { showNotification(`Para ${value} yapıldı!`, "success"); } } ], "🏎️ Racing": [ { name: "Anında Kazan", description: "Yarışı anında bitir", type: "button", run: function() { showNotification("Yarış kazanıldı!", "success"); } }, { name: "Soru Sayısı Ayarla", description: "Kalan soru sayısını değiştir", type: "input", value: "0", run: function(value) { showNotification(`Soru sayısı ${value} yapıldı!`, "success"); } } ], "⚔️ Battle Royale": [ { name: "Otomatik Cevaplama", description: "Soruları otomatik cevapla", type: "toggle", enabled: false, run: function() { showNotification(this.enabled ? "Oto cevap AÇIK" : "Oto cevap KAPALI", this.enabled ? "success" : "error"); } }, { name: "Doğru Cevabı Seç", description: "Anında doğru cevabı seç", type: "button", run: function() { showNotification("Doğru cevap seçildi!", "success"); } } ], "🚀 Blook Rush": [ { name: "Blook Sayısı Ayarla", description: "Blook sayısını değiştir", type: "input", value: "10", run: function(value) { showNotification(`Blook sayısı ${value} yapıldı!`, "success"); } }, { name: "Savunma Ayarla", description: "Savunma miktarını değiştir (Max 4)", type: "input", value: "4", run: function(value) { showNotification(`Savunma ${value} yapıldı!`, "success"); } } ], "🦕 Deceptive Dinos": [ { name: "Otomatik Seçim", description: "En iyi fosili otomatik seç", type: "toggle", enabled: false, run: function() { showNotification(this.enabled ? "Oto seçim AÇIK" : "Oto seçim KAPALI", this.enabled ? "success" : "error"); } }, { name: "Kaya ESP", description: "Kayaların altını göster", type: "toggle", enabled: false, run: function() { showNotification(this.enabled ? "Kaya ESP AÇIK" : "Kaya ESP KAPALI", this.enabled ? "success" : "error"); } }, { name: "Fosil Ayarla", description: "Fosil miktarını değiştir", type: "input", value: "100", run: function(value) { showNotification(`Fosil ${value} yapıldı!`, "success"); } }, { name: "Çarpan Ayarla", description: "Fosil çarpanını değiştir", type: "input", value: "2", run: function(value) { showNotification(`Çarpan ${value} yapıldı!`, "success"); } }, { name: "Hileyi Durdur", description: "Yakalanmamak için hileyi kapat", type: "button", run: function() { showNotification("Hile durduruldu!", "success"); } } ], "🏴‍☠️ Pirate": [ { name: "Hazine ESP", description: "Sandıkların içeriğini göster", type: "toggle", enabled: false, run: function() { showNotification(this.enabled ? "Hazine ESP AÇIK" : "Hazine ESP KAPALI", this.enabled ? "success" : "error"); } }, { name: "Max Seviyeler", description: "Tüm adaları ve gemiyi max yap", type: "button", run: function() { showNotification("Seviyeler max yapıldı!", "success"); } }, { name: "Para Ayarla", description: "Para miktarını değiştir", type: "input", value: "1000", run: function(value) { showNotification(`Para ${value} yapıldı!`, "success"); } }, { name: "Soygun Başlat", description: "Birine soygun başlat", type: "input", value: "Oyuncu Adı", run: function(value) { showNotification(`${value} soyuluyor!`, "success"); } }, { name: "Para Çal", description: "Başkasının parasını çal", type: "input", value: "Oyuncu Adı", run: function(value) { showNotification(`${value} paraları çalındı!`, "success"); } }, { name: "Para Takas", description: "Biriyle para takas et", type: "input", value: "Oyuncu Adı", run: function(value) { showNotification(`${value} ile takas yapıldı!`, "success"); } } ], "🏊 Pool": [ { name: "Top Hızı", description: "Topu hızlandır", type: "button", run: function() { showNotification("Top hızlandırıldı!", "success"); } }, { name: "Mükemmel Vuruş", description: "Her zaman mükemmel vur", type: "toggle", enabled: false, run: function() { showNotification(this.enabled ? "Mükemmel vuruş AÇIK" : "Mükemmel vuruş KAPALI", this.enabled ? "success" : "error"); } } ], "👾 Monster Brawl": [ { name: "Çift XP", description: "Düşman XP'sini 2x yap", type: "button", run: function() { showNotification("XP 2x yapıldı!", "success"); } }, { name: "Yavaş Düşmanlar", description: "Düşmanları yavaşlat", type: "button", run: function() { showNotification("Düşmanlar yavaşlatıldı!", "success"); } }, { name: "Anında Öldürme", description: "Düşmanları tek vuruşta öldür", type: "button", run: function() { showNotification("Anında öldürme aktif!", "success"); } }, { name: "Ölümsüzlük", description: "Yenilmez ol", type: "button", run: function() { showNotification("Ölümsüzlük aktif!", "success"); } }, { name: "Düşmanları Öldür", description: "Tüm düşmanları yok et", type: "button", run: function() { showNotification("Düşmanlar öldürüldü!", "success"); } }, { name: "Mıknatıs", description: "XP'leri kendine çek", type: "button", run: function() { showNotification("Mıknatıs aktif!", "success"); } }, { name: "Max Yetenekler", description: "Tüm yetenekleri max yap", type: "button", run: function() { showNotification("Yetenekler max yapıldı!", "success"); } }, { name: "Sonraki Seviye", description: "Bir sonraki seviyeye geç", type: "button", run: function() { showNotification("Seviye atlandı!", "success"); } }, { name: "Engelleri Kaldır", description: "Tüm engelleri temizle", type: "button", run: function() { showNotification("Engeller kaldırıldı!", "success"); } }, { name: "Canı Sıfırla", description: "Canı sıfırla ve 3 sn ölümsüz ol", type: "button", run: function() { showNotification("Can sıfırlandı!", "success"); } } ], "👑 Crazy Kingdom": [ { name: "Seçim ESP", description: "Seçimlerin sonuçlarını göster", type: "toggle", enabled: false, run: function() { showNotification(this.enabled ? "Seçim ESP AÇIK" : "Seçim ESP KAPALI", this.enabled ? "success" : "error"); } }, { name: "Vergi Kapat", description: "Vergi ödemeyi kapat", type: "button", run: function() { showNotification("Vergi kapatıldı!", "success"); } }, { name: "Max İstatistikler", description: "Tüm kaynakları max yap", type: "button", run: function() { showNotification("İstatistikler max yapıldı!", "success"); } }, { name: "Misafir Sayısı Ayarla", description: "Görülen misafir sayısını değiştir", type: "input", value: "100", run: function(value) { showNotification(`Misafir sayısı ${value} yapıldı!`, "success"); } }, { name: "Misafiri Geç", description: "Mevcut misafiri geç", type: "button", run: function() { showNotification("Misafir geçildi!", "success"); } } ], "🧸 Plushie Panic": [ { name: "Sonsuz Can", description: "Canın hiç bitmez", type: "toggle", enabled: false, run: function() { showNotification(this.enabled ? "Sonsuz can AÇIK" : "Sonsuz can KAPALI", this.enabled ? "success" : "error"); } }, { name: "Hızlı Koşu", description: "Daha hızlı koş", type: "button", run: function() { showNotification("Hızlı koşu aktif!", "success"); } }, { name: "Düşmanları Dondur", description: "Düşmanları yerinde dondur", type: "button", run: function() { showNotification("Düşmanlar donduruldu!", "success"); } }, { name: "Puan Ayarla", description: "Puanını değiştir", type: "input", value: "1000", run: function(value) { showNotification(`Puan ${value} yapıldı!`, "success"); } } ], "⚡ Zorblitz": [ { name: "Sonsuz Enerji", description: "Enerjin hiç bitmez", type: "toggle", enabled: false, run: function() { showNotification(this.enabled ? "Sonsuz enerji AÇIK" : "Sonsuz enerji KAPALI", this.enabled ? "success" : "error"); } }, { name: "Hızlı Saldırı", description: "Daha hızlı saldır", type: "button", run: function() { showNotification("Hızlı saldırı aktif!", "success"); } }, { name: "Güçlü Saldırı", description: "Saldırıların daha güçlü olur", type: "button", run: function() { showNotification("Güçlü saldırı aktif!", "success"); } }, { name: "Puan Ayarla", description: "Puanını değiştir", type: "input", value: "1000", run: function(value) { showNotification(`Puan ${value} yapıldı!`, "success"); } } ] }; let menuElement = null; let isMenuOpen = false; let floatingButton = null; function createFloatingButton() { floatingButton = document.createElement('div'); floatingButton.className = 'floating-button'; floatingButton.innerHTML = '<span class="floating-button-icon">🎮</span>'; let isDragging = false; let startX, startY, startLeft, startTop; floatingButton.addEventListener('mousedown', (e) => { isDragging = true; startX = e.clientX; startY = e.clientY; const rect = floatingButton.getBoundingClientRect(); startLeft = rect.left; startTop = rect.top; floatingButton.style.transition = 'none'; e.preventDefault(); }); document.addEventListener('mousemove', (e) => { if (isDragging) { const dx = e.clientX - startX; const dy = e.clientY - startY; floatingButton.style.left = startLeft + dx + 'px'; floatingButton.style.top = startTop + dy + 'px'; floatingButton.style.right = 'auto'; floatingButton.style.bottom = 'auto'; } }); document.addEventListener('mouseup', (e) => { if (isDragging) { isDragging = false; floatingButton.style.transition = 'all 0.3s'; const dx = e.clientX - startX; const dy = e.clientY - startY; if (Math.abs(dx) < 5 && Math.abs(dy) < 5) { toggleMenu(); } } }); floatingButton.addEventListener('touchstart', (e) => { isDragging = true; const touch = e.touches[0]; startX = touch.clientX; startY = touch.clientY; const rect = floatingButton.getBoundingClientRect(); startLeft = rect.left; startTop = rect.top; floatingButton.style.transition = 'none'; e.preventDefault(); }); document.addEventListener('touchmove', (e) => { if (isDragging) { const touch = e.touches[0]; const dx = touch.clientX - startX; const dy = touch.clientY - startY; floatingButton.style.left = startLeft + dx + 'px'; floatingButton.style.top = startTop + dy + 'px'; floatingButton.style.right = 'auto'; floatingButton.style.bottom = 'auto'; e.preventDefault(); } }, { passive: false }); document.addEventListener('touchend', (e) => { if (isDragging) { isDragging = false; floatingButton.style.transition = 'all 0.3s'; const touch = e.changedTouches[0]; const dx = touch.clientX - startX; const dy = touch.clientY - startY; if (Math.abs(dx) < 5 && Math.abs(dy) < 5) { toggleMenu(); } } }); document.body.appendChild(floatingButton); } function createMenu() { const container = document.createElement('div'); container.className = 'mod-menu-container'; container.id = 'modMenu'; const header = document.createElement('div'); header.className = 'mod-menu-header'; header.innerHTML = `<div class="mod-menu-title">${config.menuTitle} v${config.version}</div><button class="mod-menu-close">✕</button>`; const content = document.createElement('div'); content.className = 'mod-menu-content'; const sidebar = document.createElement('div'); sidebar.className = 'mod-menu-sidebar'; const cheatsArea = document.createElement('div'); cheatsArea.className = 'mod-menu-cheats'; let activeCategory = null; Object.keys(cheats).forEach((category, index) => { const categoryBtn = document.createElement('div'); categoryBtn.className = 'mod-menu-category'; categoryBtn.textContent = category; categoryBtn.onclick = () => { document.querySelectorAll('.mod-menu-category').forEach(c => c.classList.remove('active')); categoryBtn.classList.add('active'); showCheats(category); }; if (index === 0) { categoryBtn.classList.add('active'); activeCategory = category; } sidebar.appendChild(categoryBtn); }); function showCheats(category) { cheatsArea.innerHTML = ''; cheats[category].forEach((cheat, index) => { const card = document.createElement('div'); card.className = 'cheat-card'; card.innerHTML = `<div class="cheat-card-title">${cheat.name}</div><div class="cheat-card-description">${cheat.description}</div>`; if (cheat.type === 'toggle') { const toggle = document.createElement('div'); toggle.className = `cheat-toggle ${cheat.enabled ? 'active' : ''}`; toggle.style.position = 'absolute'; toggle.style.top = '10px'; toggle.style.right = '10px'; card.onclick = () => { cheat.enabled = !cheat.enabled; toggle.classList.toggle('active', cheat.enabled); card.classList.toggle('active', cheat.enabled); cheat.run(); }; card.appendChild(toggle); } else if (cheat.type === 'input') { const input = document.createElement('input'); input.type = 'text'; input.className = 'cheat-input'; input.placeholder = 'Değer girin...'; input.value = cheat.value || ''; input.onclick = (e) => e.stopPropagation(); input.onkeypress = (e) => { if (e.key === 'Enter') { cheat.run(input.value); card.classList.add('active'); setTimeout(() => card.classList.remove('active'), 500); } }; card.onclick = () => { if (document.activeElement !== input) { cheat.run(input.value); card.classList.add('active'); setTimeout(() => card.classList.remove('active'), 500); } }; card.appendChild(input); } else { card.onclick = () => { cheat.run(); card.classList.add('active'); setTimeout(() => card.classList.remove('active'), 500); }; } card.style.animation = `fadeIn 0.3s ease-out ${index * 30}ms both`; cheatsArea.appendChild(card); }); } showCheats(activeCategory); content.appendChild(sidebar); content.appendChild(cheatsArea); container.appendChild(header); container.appendChild(content); document.body.appendChild(container); header.querySelector('.mod-menu-close').onclick = () => { toggleMenu(false); }; let isDraggingMenu = false; let menuStartX, menuStartY, menuStartLeft, menuStartTop; header.addEventListener('mousedown', (e) => { isDraggingMenu = true; menuStartX = e.clientX; menuStartY = e.clientY; const rect = container.getBoundingClientRect(); menuStartLeft = rect.left; menuStartTop = rect.top; container.style.transition = 'none'; e.preventDefault(); }); document.addEventListener('mousemove', (e) => { if (isDraggingMenu) { const dx = e.clientX - menuStartX; const dy = e.clientY - menuStartY; container.style.left = menuStartLeft + dx + 'px'; container.style.top = menuStartTop + dy + 'px'; container.style.transform = 'none'; } }); document.addEventListener('mouseup', () => { if (isDraggingMenu) { isDraggingMenu = false; container.style.transition = 'all 0.3s'; } }); header.addEventListener('touchstart', (e) => { isDraggingMenu = true; const touch = e.touches[0]; menuStartX = touch.clientX; menuStartY = touch.clientY; const rect = container.getBoundingClientRect(); menuStartLeft = rect.left; menuStartTop = rect.top; container.style.transition = 'none'; e.preventDefault(); }); document.addEventListener('touchmove', (e) => { if (isDraggingMenu) { const touch = e.touches[0]; const dx = touch.clientX - menuStartX; const dy = touch.clientY - menuStartY; container.style.left = menuStartLeft + dx + 'px'; container.style.top = menuStartTop + dy + 'px'; container.style.transform = 'none'; e.preventDefault(); } }, { passive: false }); document.addEventListener('touchend', () => { if (isDraggingMenu) { isDraggingMenu = false; container.style.transition = 'all 0.3s'; } }); return container; } function toggleMenu(show) { if (!menuElement) { menuElement = createMenu(); } if (show === undefined) { show = !isMenuOpen; } isMenuOpen = show; if (show) { menuElement.classList.add('show'); if (floatingButton) { floatingButton.style.display = 'none'; } } else { menuElement.classList.remove('show'); if (floatingButton) { floatingButton.style.display = 'flex'; } } } document.addEventListener('keydown', (e) => { if (e.ctrlKey && e.key.toLowerCase() === 'e') { e.preventDefault(); toggleMenu(); } }); function initialize() { console.log('%c🎮 Modern Blooket Mod Menüsü', 'color: #a855f7; font-size: 20px; font-weight: bold;'); console.log('%cVersiyon: ' + config.version, 'color: #c4b5fd; font-size: 14px;'); console.log('%cTüm oyunlar için hileler yüklendi!', 'color: #22c55e; font-size: 14px;'); createFloatingButton(); createMenu(); toggleMenu(true); showNotification('Mod menüsü yüklendi! 🎮', 'success'); } if (document.readyState === 'loading') { document.addEventListener('DOMContentLoaded', initialize); } else { initialize(); } })();
